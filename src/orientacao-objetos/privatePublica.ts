// class jogoComDescricao extends Jogo {
//     private descricao: string;
//     constructor(nome: string, preco: number, descricao: string) {
//         super(nome, preco);
//         this.descricao = descricao;
//     }
// }

// public: acessível de forma geral, dentro de fora da classe
// private: é acesível apenas dentro da classe onde o campo foi criado
// protected: é acessível apenas dentro da classe e subclasse onde o campo foi criado
// interfaces -> são classes que não possuem implementação, são apenas contratos
// se não declaramos algo, eles serão publicos por padrão