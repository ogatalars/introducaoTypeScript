let nome: string;

//...
nome = 'Felipe'

const nomeComValor = 'Ogata' // consegue entender que a tipagem é string 

function nomeCompleto(nome: string, sobrenome: string) {
    return `${nome} ${nomeComValor}`
}